# Image3D Dev Script
# Runs the development server with live reload and auto-build (writeToDisk enabled)

Write-Host "Starting Image3D Development Environment..." -ForegroundColor Cyan
Write-Host "The application will auto-build to /dist and serve at http://localhost:9000" -ForegroundColor Gray

npm run dev
